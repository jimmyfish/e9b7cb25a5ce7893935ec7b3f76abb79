{% extends 'subLayout.html.twig' %}

{% block title %}Edit Validasi{% endblock %}

{% block content %}
    <div class="content__header">
        <h2><center>Edit Active</center></h2>
    </div>
    <div class="card">
        <div class="card__body">
            <form method="post">
                <div class="form-group form-group--centered form-group--centered">
                    <input type="radio" id="radio1" name="validate" value="0" {% if data.isActive == 0 %}checked="checked"{% endif %}>
                    <label for="radio1">Sudah Tidak Aktif</label>
                </div>
                <div class="form-group form-group--centered form-group--centered">
                    <input type="radio" id="radio2" name="validate" value="1"{% if data.isActive == 1 %}checked="checked"{% endif %}>
                    <label for="radio2">Masih Aktif</label>
                </div>
                <button class="btn btn-primary btn-block">SIMPAN</button>
            </form>
        </div>
    </div>
{% endblock %}